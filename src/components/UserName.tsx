//Temporary file created for positioning.

const UserName: React.FC = () => {
let inline={
    display:'inline'
}

    return (
        <div style={inline}> User Name</div>
     );
};

export default UserName;